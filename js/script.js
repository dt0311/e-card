// Example JavaScript code
document.getElementById('D:/Arani2/Background E-Card.png').addEventListener('click', function(event) {
    event.preventDefault(); // Prevents the default action of the link (if href="#" is used)
    // Add your custom functionality here, like changing the image source
    // Example: document.querySelector('img').src = 'newimage.jpg';
});
